environment:
  workflow_name: "upload-pipeline-async"
  gateway: "gateway:8080"
  enable_tracing: false
  enable_hmac: false
  intermediate_storage: true # use datastore as a intermediate storage
  s3_url: "minio:9000"
  s3_tls: false
